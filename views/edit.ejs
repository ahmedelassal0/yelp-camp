<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= campground.title %></title>
</head>
<body>
    <form action="/campgrounds/<%= campground.id %>?_method=PUT" method="post">
        <input type="text" name="campground[title]" placeholder="title" value="<%= campground.title %>">
        <input type="text" name="campground[location]" placeholder="location" value="<%= campground.location %>">
        <button>submit</button>
    </form>
    <a href="/campgrounds/<%= campground.id %>">cancel</a>
</body>
</html>